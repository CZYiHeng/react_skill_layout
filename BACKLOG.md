# 待办与已知问题

## BUG

### 自动闸门仍会提问导致卡死
- **现象**：前端闸门选"自动"后，Agent 遇到 ASK/gate 时仍会停下来等用户确认，任务卡死
- **期望**：自动模式下不应阻塞
- **优化方向**：调试好 skill 后，gate 弹出后若用户 30 秒内未选择，自动确认继续（超时放行）
- **状态**：已修复（commit 65746c3，auto 模式 gate 弹出 30s 倒计时自动 continue，纠偏输入聚焦时暂停）

## FEAT

### 审查报告渲染优化
- **现象**：审查结果目前用 `<pre>` 原样展示模型返回的 Markdown 全文（深色终端块），标题/表格/列表/勾选符号/加粗都是纯文本字符，阅读费眼
- **期望**：报告作为结构化内容渲染——标题层级、表格（逐项对照表）、列表、通过/偏差用色块/图标、代码块区分；保留"复制/导出 Markdown"
- **优化方向**：引入轻量 markdown 渲染（如 marked + DOMPurify），对"逐项对照"表格、"偏差项"等固定段落做样式增强；不引重型编辑器
- **状态**：已完成（marked + DOMPurify 结构化渲染，含复制/导出按钮，标题/表格/列表/代码块/引用样式）


# react-agent 使用指南（本机速查）

> 针对你的机器（Windows + 已装 Python/uv/node）整理。核心一句话：**打开浏览器就能用**。

---

## 一、现在就能用（服务已在运行）

打开浏览器访问：**http://127.0.0.1:8000**

- 这就是 Web 对话界面（五阶段分色面板、流式输出、人工步进）
- 当前后端配置：`config.deepseek.json`（模型 `deepseek-chat`）
- 若打不开，见下方「三、启动 / 停止 / 重启」

## 二、日常操作

| 你想做的事 | 怎么操作 |
|---|---|
| 发任务 | 底部输入框输入自然语言任务，回车 |
| 计划确认（gate 拦截） | 计划生成后出现「继续 / 纠偏 / 中止」，选继续即放行 |
| 模型提问（ASK） | 弹提问框时输入答案，回答后自动继续（跳过回答也可以） |
| 中途插手 | 随时点「暂停」，循环在阶段边界停下；或直接纠偏，从下一步生效 |
| 换工作目录 | 顶部状态栏「目录」输入框（只认项目内子目录；目录不存在自动回退） |
| 切换 gate_mode | `plan`（推荐，计划确认一次）/ `step`（每步拦）/ `auto`（尽量不拦） |
| 导出会话 | 顶部「导出纪要」下载 Markdown |

## 三、启动 / 停止 / 重启

```bat
start.bat            :: 双击 = 启动 Web（后端 8000 + 自动构建前端 + 开浏览器）
start.bat dev        :: 开发模式（前端热更新 5173）
start.bat cli        :: 终端 REPL（等同 react-agent.bat）
start.bat stop       :: 停止后台服务
start.bat help       :: 查看帮助
```

> ⚠️ 改配置后必须重启后端才生效：先 `start.bat stop`，再 `start.bat`。

## 四、换模型 / 换 key

编辑 `config.deepseek.json`（或 `config.json`）三个字段：

```json
{
  "base_url": "https://api.deepseek.com",
  "api_key": "sk-xxx",
  "model": "deepseek-chat"
}
```

- `start.bat` 默认读 `config.deepseek.json`；想换档案：`set REACT_AGENT_CONFIG=config.json` 后再启动
- 也可以用环境变量覆盖（优先级最高）：`REACT_AGENT_API_KEY` / `REACT_AGENT_MODEL` / `REACT_AGENT_BASE_URL`
- 模型支持 OpenAI 兼容端点：deepseek / kimi / 通义 / Moonshot 等

## 五、想改行为 —— 改 skill 文件即可（换文件即换行为）

```
skills\think\SKILL.md      ← THINK 阶段：怎么决策下一步
skills\plan\SKILL.md       ← PLAN 阶段：怎么拆步骤
skills\act\SKILL.md        ← ACT 阶段：怎么产出产物
skills\observe\SKILL.md    ← OBSERVE 阶段：怎么核对
skills\verify\SKILL.md     ← VERIFY 阶段：怎么验收
```

改完重启即生效。项目还带一套写代码专用档案 `skills_code\`（8 字段函数头、增量骨架等），用下面方式切换：

```bat
set REACT_AGENT_SKILLS_DIR=G:\react-agent\skills_code
start.bat cli
```

## 六、想改代码 —— 修改地图

| 想改什么 | 改哪个文件（关键位置） |
|---|---|
| 循环逻辑 / 判定规则 / 闸门 | `react\loop.py`（ReActLoop、_resolve、_apply_gate） |
| 模型调用 / 重试 / 流式 | `react\model.py`（OpenAIClient、_permanent_status） |
| 内置默认提示词 / 槽位 | `react\action.py`（DEFAULT_PROMPTS、ActionRegistry.load） |
| 上下文窗口 / 历史摘要 | `react\context.py`（max_context_messages、_window_start） |
| 真实执行 / 写文件 | `react\executor.py`（LocalExecutor） |
| Windows 沙箱 | `react\win32_sandbox.py` |
| 配置项 / 默认值 | `react\config.py`（DEFAULTS、ENV_MAP） |
| 终端渲染 / 展示模板 | `react\render.py` + `react\display.py` |
| Web 界面 | `web\src\*`（App.jsx 状态机、components\*） |
| Web 后端 API | `react\webapi.py` |
| 配置开关 | `config.json`：`enable_shell_exec` / `enable_file_write` / `sandbox_shell` / `gate_mode` / `max_rounds` |

改完代码后验证：`uv run python main.py --smoke`（Mock 模型，零 API 消耗）。

## 七、执行能力（默认关闭，按需打开）

想让 agent 真的跑命令 / 写文件，在配置文件加：

```json
{
  "enable_shell_exec": true,
  "enable_file_write": true,
  "exec_timeout_sec": 30,
  "sandbox_shell": false
}
```

- 打开后 ACT 产物里的 `[EXEC: shell|write]` 才会真实执行（越界路径拒绝）
- Windows 可加 `sandbox_shell: true` 套 OS 级沙箱（剥特权+作业对象）

## 八、安全提醒

- ⚠️ `config.json` / `config.deepseek.json` 里的 `api_key` 是**明文真实 key**，别把这个文件发给别人 / 提交到公开仓库
- 建议改用环境变量：`set REACT_AGENT_API_KEY=sk-xxx`，并把明文 key 从配置文件删掉
